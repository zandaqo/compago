{
  "compilerOptions": {
    "experimentalDecorators": true,
    "lib": [
      "dom",
      "dom.iterable",
      "dom.asynciterable",
      "deno.ns"
    ]
  },
  "tasks": {
    "test": "deno test -A --no-check --location https://example.com ./tests",
    "build": "deno run -A --no-check _build.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["ban-types"]
    }
  }
}
